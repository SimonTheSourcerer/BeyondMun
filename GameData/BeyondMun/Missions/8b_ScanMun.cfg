CONTRACT_TYPE:NEEDS[SCANsat]
{
    name = BMScanMun

    // Contract text
    title = [BM] Scan Mun
    description = We are required to create Altimetry scan of Muns surface.\n This will greatly enhance our knowledge about our moon.
    synopsis = Reach a fitting orbit and scan Muns surface.
    completedMessage = KDO will be statisfied!

    // Controls for whether a contract can be declined or cancelled, default is true for both
    cancellable = true
    declinable = true

    // Target Celestial body - controls where the contract must be completed, has some automated
    // effects on numeric parameters (increasing science, etc.)
    targetBody = Mun

    // Contract rewards
    rewardScience = 5000.0
	rewardReputation = 20.0
    rewardFunds = 100000.0
	
	// Can only be done once
	maxCompletions = 1
	maxSimultaneous = 1
	
	prestige = Trivial
		
	agent = Integrated Integrals	

	PARAMETER
	{
		name = VesselParameterGroup1
		type = VesselParameterGroup
			
		PARAMETER
		{
			name = HasCrew1
			type = HasCrew
			maxCrew = 0
			minCrew = 0
		}
		
		PARAMETER
		{
			name = HasPart1
			type = HasPart
			
			part = SCANsat_Scanner
			minCount = 1
		}
	}
		
	PARAMETER
	{
		name = VesselParameterGroup1
		type = VesselParameterGroup
		
		duration = 4d
		
		title = Hold the satellite in position to completely scan the surface.
		
		PARAMETER
		{
			name = ReachSpecificOrbit1
			type = ReachSpecificOrbit

			index = 0
		}
	}
		
	PARAMETER
	{
		name = SCANsatCoverage1
		type = SCANsatCoverage

		coverage = 85.0
		scanType = AltimetryLoRes
	}
	
	REQUIREMENT
	{
		name = SCANsatCoverage
		type = SCANsatCoverage

		targetBody = Mun

		// Maximum coverage that must be reached before the contract is valid.
		maxCoverage = 84.9
		minCoverage = 0.0		

		// The type of scan to perform.  Valid values are from SCANdata.SCANtype.
		scanType = AltimetryLoRes
	}
	
	REQUIREMENT
	{
		name = CompleteContract1
		type = CompleteContract
		minCount = 1
		contractType = BMOrbitMun
	}
	
	REQUIREMENT
	{
		name = CompleteContract1
		type = CompleteContract
		minCount = 1
		contractType = BMScanKerbal
	}
}