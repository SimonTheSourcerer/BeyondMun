// This contract shows all the different parameters that can be used.
CONTRACT_TYPE
{
    name = BMRoverTest

    // Contract text
    title = [BM] Remote Controlled Rover Test
    description = We thinking about creating a new series of probes. This time we want to ensure that the probes can do more than sitting on surface. We develop wheels that should work on other planets.
    synopsis = Design and test a remote controlled rover for our first visit to Minmus!
    completedMessage = Nice, Minmus is waiting!

    // Controls for whether a contract can be declined or cancelled, default is true for both
    cancellable = true
    declinable = true

    // Target Celestial body - controls where the contract must be completed, has some automated
    // effects on numeric parameters (increasing science, etc.)
    targetBody = Kerbin

    // Contract rewards
    rewardScience = 20.0
	rewardReputation = 20.0
    rewardFunds = 15000.0
	
	// Can only be done once
	maxCompletions = 1
	maxSimultaneous = 1
	
	prestige = Trivial

	PARAMETER
	{
		name = VesselParameterGroup1
		type = VesselParameterGroup
		
		define = Minmus Test Rover
		
		PARAMETER
		{
			name = HasCrew1
			type = HasCrew

			minCrew = 0
			maxCrew = 0
			
			disableOnStateChange = false
		}
		
		PARAMETER
		{
			name = VesselMass1
			type = VesselMass

			maxMass = 1.0
			
			disableOnStateChange = false
		}
		
		PARAMETER
		{
			name = PartValidation1
			type = PartValidation
			part = roverWheel2
			minCount = 4			
		}
		
		PARAMETER
		{
			name = ReachState
			type = ReachState

			situation = PRELAUNCH
		}
	}

	PARAMETER
	{
		name = VesselParameterGroup1
		type = VesselParameterGroup
		
		vessel = Minmus Test Rover
		
		PARAMETER
		{
			name = ReachState
			type = ReachState

			biome = Tracking Station
			
			disableOnStateChange = False	

			title = Visit the Tracking Station
		}
	}
	
	PARAMETER
	{
		name = VesselParameterGroup1
		type = VesselParameterGroup
		
		vessel = Minmus Test Rover
		
		PARAMETER
		{
			name = ReachState
			type = ReachState

			biome = R&D 
			
			disableOnStateChange = False	

			title = Visit R&D
		}
	}

		PARAMETER
	{
		name = VesselParameterGroup1
		type = VesselParameterGroup
		
		vessel = Minmus Test Rover
		
		PARAMETER
		{
			name = ReachState
			type = ReachState

			biome = Mission Control
			
			disableOnStateChange = False	

			title = Visit Mission Control
		}
	}
	
	PARAMETER
	{
		name = VesselParameterGroup1
		type = VesselParameterGroup
		
		vessel = Minmus Test Rover
		
		PARAMETER
		{
			name = ReachState
			type = ReachState

			biome = Runway 
			
			minSpeed = 8.0
			
			disableOnStateChange = False	

			title = Do sprint on the Runway and get the Rover to a minimum speed of 8.0 m/s
		}
	}
	
	REQUIREMENT
	{
		name = CompleteContract1
		type = CompleteContract
		minCount = 1
		
		contractType = ScanMinmus
	}
}